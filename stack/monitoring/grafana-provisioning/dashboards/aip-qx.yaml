apiVersion: 1

providers:
  - name: qx
    orgId: 1
    # Empty folder = root: the dashboard appears directly in the Dashboards
    # list with no enclosing folder. A previous `folder: AIP Qx` + `folderUid: qx`
    # caused a click-through bug because the folder UID collided with the
    # dashboard UID (both `qx`).
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
