.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
*.pfx
network/wallets/
network/crypto-config/
deployment/keycloak/tls/
deployment/keycloak/themes/
deployment/keycloak/.env.kc

logs/
*.log

__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
.venv/
venv/
ENV/

*.xml
*_results.txt
out*.txt
failures*.txt
pytest_*.txt
coverage_out.txt
summary.txt

.DS_Store
Thumbs.db
.vscode/
.idea/

/tmp/rwa-reports/
audit_report.pdf
audit_report.tex

templates/

*.exe

# Local tool settings and sensitive mock data
.claude/
.ruff_cache/
.mypy_cache/
tests/fixtures/crypto_config_mock/

# Deployment / packaging archives that must never reach the repo
*.tar.gz
*.tgz
*.zip

# LaTeX build by-products only — actual .tex sources under docs/
# are part of the project and must remain trackable.
/doc.tex
*.aux
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk

# Local deploy scripts (contain credentials)
scripts/_deploy_*.py

# Grafana customization scripts (contain VM credentials — never commit)
grafana_scripts/

# Node dependencies and npm files (grafana/tooling scripts only)
node_modules/
package.json
package-lock.json

# Standalone ASCII tools (never commit)
ascii2.py
ascii_standalone.py
