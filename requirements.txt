# Pinned to exact versions for reproducible deployments. Bump intentionally
# in a separate commit and run the security review skill before merging.

fastapi==0.135.1
uvicorn==0.42.0

SQLAlchemy==2.0.48
asyncpg==0.31.0
alembic==1.14.0

pydantic==2.12.5
pydantic-settings==2.13.1

cryptography==46.0.5
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.5.0
bcrypt==5.0.0

redis==7.3.0
celery==5.6.2

grpcio==1.78.0
grpcio-reflection==1.78.0
# grpcio-tools is a build-time codegen tool — install ad-hoc to regenerate stubs:
#   pip install grpcio-tools && bash scripts/generate_protos.sh
protobuf==6.31.1
PyYAML==6.0.3

reportlab==4.4.10
rapidfuzz==3.14.3

prometheus-client==0.24.1
prometheus-fastapi-instrumentator==7.1.0

hvac==2.4.0
neo4j==6.1.0

chromadb==1.5.8
groq==1.2.0

py_ecc==8.0.0
pyotp==2.9.0

# pybind11 is required to build the helib_ckks native extension.
# HElib itself is a C++ library installed separately (see backend/features/fhe/CMakeLists.txt).
pybind11>=2.13.0
