# Pxtly CLI — runtime dependencies
#
# Install with:
#   pip install -r cli/requirements.txt
#
# This file is intentionally separate from the project requirements.txt at
# the repo root: that one is the API/backend runtime. The CLI is a
# standalone tool that ships its own pinned set.

# Core framework
typer>=0.12,<1.0          # CLI argparser + sub-app composition
rich>=13.7,<14.0          # Console rendering (tables, status, markdown)
prompt_toolkit>=3.0,<4.0  # REPL with history + key bindings

# Transport
httpx>=0.27,<1.0          # Async HTTP client (used by every cli/api/* module)
websockets>=12.0,<14      # Only by legacy code paths; safe to remove later

# Settings + validation
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0

# Secure token storage
keyring>=24.0,<26         # OS-native credential vault (Windows CredMan, macOS Keychain, Secret Service)

# Optional — only required by `pxtly dashboard`
textual>=0.83,<1.0
